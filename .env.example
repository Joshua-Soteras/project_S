# ============================================
# PostgreSQL Docker Configuration
# ============================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to create your local .env file:
#    cp .env.example .env
#
# 2. Edit .env and replace the placeholder password with your own
#
# 3. Start the database container:
#    docker-compose up -d postgres
#
# 4. Verify it's running:
#    docker ps
#
# NOTE: The .env file is gitignored and will NOT be committed.
#       Each developer maintains their own local .env file.
# ============================================

# PostgreSQL superuser username
POSTGRES_USER=projects_admin

# PostgreSQL superuser password
# IMPORTANT: Change this to a secure password!
POSTGRES_PASSWORD=your_password_here

# Database name (created automatically on first run)
POSTGRES_DB=projects_db
