{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "NlpService": {
    // Docker service name — used when the API runs inside docker-compose
    "BaseUrl": "http://nlp:8000"
  }
}
